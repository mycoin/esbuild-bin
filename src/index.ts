import buildContext from "./buildContext";
import normalize from "./normalize";
import parseArgs from "./parseArgs";
import run from "./run";

export {
  // 定义配置
  buildContext,
  // 归一化配置
  normalize,
  // 解析命令行参数
  parseArgs,
  // 运行构建
  run,
};
